name: Publish Release to PyPI (pluto-ml)

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 0.1.0). Required for manual trigger.'
        required: true

permissions: {}

jobs:
  release-build-pypi:
    # Isolate PyPI publishing from secrets on other branches. The "build"
    # environment can be restricted (via repo settings) to protected branches
    # so rogue workflows on feature branches can't reach its secrets/OIDC.
    environment: build
    permissions:
      contents: read
      id-token: write  # OIDC for PyPI Trusted Publishing
    runs-on: ubicloud-standard-2
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.10"

      - name: Install Poetry
        run: pip install poetry

      - name: Extract version from release tag
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${{ github.event.release.tag_name }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Resolved version: $VERSION"

      # Version bumps are committed manually before tagging a release.
      # This only sets the version in the runner checkout so the built
      # artifact matches the release tag; nothing is pushed back.
      - name: Set version and commit SHA for build
        run: |
          VERSION=${{ steps.version.outputs.version }}
          sed -i "0,/version = \".*\"/s//version = \"${VERSION}\"/" pyproject.toml
          sed -i "s/{{PLUTO_COMMIT_SHA}}/${{ github.sha }}/g" pluto/__init__.py
          sed -i "s/__version__ = '.*'/__version__ = '${VERSION}'/g" pluto/__init__.py

      - name: Build package
        run: poetry build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b  # v1.14.0
